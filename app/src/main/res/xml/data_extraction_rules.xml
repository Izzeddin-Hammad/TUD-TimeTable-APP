<?xml version="1.0" encoding="utf-8"?><!--
   Backup and device-transfer rules for API 31+ (see backup_rules.xml for older releases).

   Backup is switched off in the manifest (`android:allowBackup="false"`); these rules keep that
   honest if it is ever re-enabled. Everything the app stores is the student's own data — the cached
   timetable (lecturer names, rooms, class groups), bookmarked courses and search history — and
   PRIVACY.md promises it stays on the device, so nothing is included in either cloud backup or
   device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
